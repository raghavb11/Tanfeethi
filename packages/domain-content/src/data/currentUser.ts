/** The signed-in employee. In a real app this comes from auth; the prototype
 *  uses a single fixed identity so per-user features (own feedback, policy
 *  acknowledgement) behave consistently. */
export const CURRENT_USER = { id: "u-khalid", name: "Khalid", who: "Khalid A.", initials: "KH" }
